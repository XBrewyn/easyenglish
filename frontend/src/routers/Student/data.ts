import { Tab } from '../../components/Navigator/type';

const tabs: Tab[] = [
  { path: './', value: 'course' },
  { path: './profile', value: 'profile' },
  { path: './close', value: 'close sesión' },
];

export { tabs };
